{
  "name": "@solawi/server-cf",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc --build",
    "dev": "wrangler dev",
    "deploy": "cd ../.. && npm run build && node scripts/check-assets.mjs && npm run schema && node scripts/apply-schema.mjs && cd packages/server-cf && wrangler deploy",
    "deploy:worker-only": "wrangler deploy",
    "db:local": "node ../../scripts/apply-schema.mjs --local"
  },
  "dependencies": {
    "@solawi/app": "*",
    "@solawi/kernel": "*",
    "@solawi/platform-cf": "*",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "wrangler": "^4.86.0"
  }
}